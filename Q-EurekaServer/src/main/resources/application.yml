spring:
  application:
    name: eurekaServer
server:
  port: 9080
eureka:
  client:
    #不向注册中心注册自己
    register-with-eureka: false
    #不需要检索服务
    fetch-registry: false
  server:
    enable-self-preservation: false
